<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kh.drommetur.customer.mapper.CustomerMapper">

	<resultMap type="Question" id="QuestionResultSet">
		<result property="questionNo" column="QUESTION_NO" />
		<result property="category" column="CATEGORY" />
		<result property="questionTitle" column="QUESTION_TITLE" />
		<result property="questionContent" column="QUESTION_CONTENT" />
		<result property="datetime" column="DATETIME" />
		<result property="status" column="STATUS" />
	</resultMap>

	<resultMap type="Notice" id="NoticeResultSet">
		<result property="noticeNo" column="NOTICE_NO" />
		<result property="noticeTitle" column="NOTICE_TITLE" />
		<result property="noticeContent" column="NOTICE_CONTENT" />
		<result property="visit" column="VISIT" />
		<result property="datetime" column="DATETIME" />
		<result property="status" column="STATUS" />
	</resultMap>


	
	<!-- 문의 내역 -->
	<insert id="insertQuestion">
		INSERT INTO QUESTION
		VALUES(SEQ_QUESTION.NEXTVAL,#{question.category},#{question.questionTitle},#{question.questionContent},SYSDATE,DEFAULT,#{memberNo})
	</insert> 
	
	<select id="selectquestionList" resultMap="QuestionResultSet">
	SELECT *
	FROM QUESTION
	WHERE
	STATUS='Y'
	OREDER BY QUESTION_NO DESC
	</select>
	
	
	<select id="selectquestion" resultMap="QuestionResultSet">
	select questionNo, questionTitle, datetime
	from question
	where
	status='Y'
	and question_no=#{questionNo}
	</select>
	
	
	
	
	
	
	<!-- 답변내역 -->

	<!-- 공지 내역 -->
	<select id="selectListCount" resultType="_int">
		SELECT COUNT(*)
		FROM
		NOTICE
		WHERE STATUS='Y' 
	</select>

	<select id="selectList" resultMap="NoticeResultSet">
		SELECT *
		FROM NOTICE
		WHERE
		STATUS='Y'
		ORDER BY NOTICE_NO DESC
	</select>

	<insert id="insertNotice" parameterType="Notice">
		INSERT INTO NOTICE
		VALUES(SEQ_NOTICE.NEXTVAL, #{noticeTitle},
		#{noticeContent},
		#{visit}, SYSDATE,
		DEFAULT)
	</insert>
	
	<update id = "updateIncreaseCount" parameterType="_int">
	UPDATE NOTICE
	SET COUNT = COUNT +1
	WHERE NOTICE_NO = #{noticeNo}
	</update>
	
	<select id="selectNotice" resultMap="NoticeResultSet">
	SELECT *
	FROM NOTICE
	WHERE
	STATUS='Y'
	AND BOARD=#{noticeNo}
	</select>
	
	<update id="deleteNotice" parameterType="_int">
	UPDATE NOTICE
	SET STATUS='N'
	WHERE NOTICE_NO=#{noticeNo}
	</update>
	
	<update id="updateNotice" parameterType="Notice">
	UPDATE NOTICE
	SET NOTICE_TITLE=#{noticeTitle}, BOARD_CONTENT=#{noticeContent},
	WHERE NOTICE_NO = #{noticeNo}
	</update>
</mapper>
